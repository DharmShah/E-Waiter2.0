// code to push the code on github
	// git init
	// git add .
	// git remote remove origin
	// git remote -v //(aagar kai aave chee run 1 command else ready to go)
	// git remote add origin 
	// git commit -m "commits name" 
	// git branch -M main
	// git push -u origin main
	// or
	// git push -u origin main --force

// code to fetch the changes made in latest commit

	// git branch
	// git fetch origin
	// git pull origin main
